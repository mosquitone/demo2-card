{
  "name": "demo01-card",
  "version": "1.0.0",
  "description": "Source of mosquitone Demo#01 Card",
  "main": "build.js",
  "dependencies": {
    "puppeteer": "^1.19.0",
    "serve": "^11.1.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "test -d build && rm -rf build/ || exit 0",
    "build": "mkdir -p build/ && node build.js"
  },
  "author": "",
  "license": "ISC"
}
